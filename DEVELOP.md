# DEVELOP.md

LLM 코딩 순서를 확정하기 위한 지침.

**코드리서치 → 계획 → 디자인 → 구현 → 반영.**

구현 단계 전까지 실행 코드는 작성하지 마라. 테스트 코드와 디자인 시안은 작성해도 된다.

## 1. 코드리서치

**깊이 조사하라. 세부 사항을 남기지 마라.**

구현하기 전:

- 구현하고자 하는 주제를 깊이 연구하라.
- 구현 방법에 대해서 자세히 조사하라.
- 모든 세부 사항을 파악하라.
- 이미 구현된 API, 라이브러리, 오픈소스를 적극 활용하라.

**끝나면 RESEARCH.md 파일에 상세 보고서를 작성하라.**

## 2. 계획

**접근 방식을 문서로 확정하라. 상충 관계를 드러내라.**

PLAN.md를 작성할 때:

- RESEARCH.md 문서의 내용을 참고하라.
- 코드를 구현하는 접근 방식을 상세히 설명하라.
- 상충 관계를 포함하여 작성하라.
- 체크리스트로 작성하여 추후에 활용할 수 있도록 하라.

## 3. 디자인

**도구를 적극 활용하라. 전체 디자인은 클로드 디자인으로 통일하라.**

디자인할 때:

- PLAN.md의 화면과 기능 범위를 먼저 확인하라.
- 제작하고자 하는 주제에 맞는 디자인 도구를 골라라.
- 피그마, 캔바 등의 커넥터, 플러그인, 스킬을 적극 활용하여 작성하라.
- 전체적인 디자인은 클로드 디자인으로 작성하라.

화면을 구성할 때:

- 색, 타이포, 간격, 컴포넌트를 먼저 정하고 화면에 적용하라.
- 여백, 글자 크기, 요소 크기는 비율 관계를 최대한 활용하여 정하라.
- 대상 플랫폼의 관례(Material, HIG, 웹)를 따르라.
- 빈 상태, 로딩, 오류, 긴 텍스트까지 디자인하라.
- 명암비와 터치 영역 등 접근성 기준을 지켜라.
- 구현 비용이 큰 디자인은 대안과 함께 제시하라.

사용자 편의성을 고려할 때:

- 화면마다 목적을 하나로 정하고, 그 목적에 맞는 것을 가장 크고 위에 두어라.
- 사용자가 가장 자주 찾는 정보를 첫 화면에서 스크롤 없이 보이게 하라.
- 자주 누르는 동작은 손이 닿는 곳에, 되돌릴 수 없는 동작은 손이 닿기 어려운 곳에 두어라.
- 주요 기능은 두 번의 조작 안에 닿게 하라.
- 같은 기능은 모든 화면에서 같은 자리에 두어라.
- 입력을 줄여라. 기본값, 최근 항목, 자동 계산으로 대체할 수 있으면 대체하라.
- 확인 창으로 막기보다 되돌리기를 제공하라.
- 화면을 옮기지 않고 끝낼 수 있는 작업은 옮기지 마라.
- 모든 조작에 즉시 반응하라. 기다림이 필요하면 무엇을 기다리는지 밝혀라.
- 오류 메시지를 잘 쓰기 전에, 오류가 나지 않게 만들어라.

디자인 기술 스택을 정할 때:

- 디자인에 사용하는 도구, 서체, 아이콘, 차트, 토큰 전달 방식을 명시하라.
- 각 항목이 무료인지, 어떤 라이선스인지 확인하라.
- 디자인 값이 코드로 전달되는 경로를 정하라. 단일 소스를 하나만 두어라.
- 도구의 제약으로 만들지 못한 것이 있으면 우회 방법과 함께 남겨라.
- 정한 스택은 DESIGN.md와 PLAN.md 양쪽에 적어라.

DESIGN.md를 작성할 때:

- 디자인 개요를 설명하라.
- 어떤 느낌을 살리고자 했는지 의도를 밝혀라.
- 구현 가능한 값(hex, px, 폰트명)으로 적어라.
- 사용자 편의성 항목과 화면 배치 근거를 적어라.
- 디자인 기술 스택을 적어라.
- 마지막에 디자인 산출물을 넣거나 도구 결과물 링크를 걸어라.
- 구현 중 디자인이 바뀌면 갱신하라.

## 4. 구현

**계획대로 전부 구현하라. 끝날 때까지 멈추지 마라.**

구현할 때:

- 계획의 내용을 바탕으로 전부 구현하라. **PLAN.md, DESIGN.md의 내용과 동일하게 구현하라.**
- 각 코드의 채택 이유와 내용을 주석으로 작성하라.
- 작업을 완료하면 계획 문서에서 완료로 표시하라.
- 모든 작업과 단계가 종료될 때까지 멈추지 마라.

## 5. 반영

**코드를 수정해서 변경 사항이 생기면 Git으로 push하라.**

원격 저장소는 https://github.com/closeonjae/ChessPuzzle.git 이며, main 브랜치에만 push한다.

커밋할 때:

- 변경된 파일을 확인하고 의미 있는 작업 단위로 커밋하라.
- 커밋 메시지에 무엇을, 왜 바꿨는지 적어라.
- 요청과 무관한 변경 사항은 커밋에 포함하지 마라.

push할 때:

- push 전에 대상 브랜치가 맞는지 확인하라.
- 원격에 변경이 있으면 먼저 pull하여 동기화하라.
- push 후 반영 결과를 확인하고 보고하라.
