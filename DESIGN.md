# DESIGN.md

PLAN.md의 화면 범위(로그인 화면, 퍼즐 화면 2개)에 대한 디자인 확정 문서.

## 1. 개요 및 의도

- 대상: 갤럭시 워치 8, 1.47" 원형 AMOLED, 480×480px.
- 느낌: 체스판 자체는 클래식하고 차분한 원목톤(가독성 우선)으로, 그 외 앱 크롬(배경·버튼·상태 텍스트)은 따뜻한 뉴트럴 톤 + 소량의 클레이(terracotta) 포인트 컬러로 통일해 "Claude" 디자인 언어의 절제되고 따뜻한 인상을 준다.
- OLED 워치이므로 배경은 순검정에 가깝게 유지해 소비전력과 대비를 동시에 확보한다.
- 화면마다 목적 하나: 로그인 화면은 "로그인"만, 퍼즐 화면은 "지금 둘 수를 파악하고 두는 것"만 최상단에 크게 배치.

## 2. 색상 (Hex)

**앱 크롬**
| 토큰 | 값 | 용도 |
|---|---|---|
| `background` | `#000000` | 화면 배경(OLED) |
| `surface` | `#1C1B1A` | 상태 칩/오버레이 배경 |
| `surfaceAlpha85` | `rgba(28,27,26,0.85)` | 보드 위에 얹는 상태 칩(반투명) |
| `textPrimary` | `#F5F1EA` | 주요 텍스트(대비 배경 대비 ≈18.6:1) |
| `textSecondary` | `#A69C8D` | 보조 텍스트(레이팅 등, 대비 ≈7:1) |
| `accent` | `#C15F3C` | 기본 버튼, 선택 표시 |
| `success` | `#4C9A6A` | 정답 피드백 |
| `error` | `#D14343` | 오답/네트워크 오류 피드백 |

**체스판**
| 토큰 | 값 | 용도 |
|---|---|---|
| `boardLight` | `#EDEAE1` | 밝은 칸 |
| `boardDark` | `#7C6A58` | 어두운 칸 |
| `pieceWhiteFill` / `pieceWhiteStroke` | `#FFFFFF` / `#1A1816` | 흰 기물(밝은/어두운 칸 모두에서 판독 위해 반대색 1.5dp 외곽선 고정) |
| `pieceBlackFill` / `pieceBlackStroke` | `#1A1816` / `#FFFFFF` | 검은 기물(동일 규칙) |
| `selectedSquare` | `rgba(193,95,60,0.45)` | 선택한 기물의 칸 |
| `legalDot` | `rgba(245,241,234,0.55)` | 이동 가능한 칸(작은 점) |
| `lastMoveOutline` | `rgba(193,95,60,0.9)`, 2dp | 방금 둔 수 강조 |

## 3. 타이포그래피

시스템 기본 서체(Wear OS 기본, Roboto Flex 계열) 그대로 사용 — 커스텀 폰트 임베딩은 2화면짜리 개인 앱에는 비용 대비 이득이 낮아 채택하지 않음(대안 기록).

| 토큰 | 크기/굵기 | 용도 |
|---|---|---|
| `turnLabel` | 13sp / SemiBold (초안 20sp에서 축소 — 아래 참고) | "Correct", "White to move" 등 상단 텍스트 |
| `bodyRating` | 14sp / Regular | 현재 퍼즐 레이팅 |
| `caption` | 11sp / Regular | 부가 안내(에러 상세 등) |

> **구현 중 수정**: 상단 텍스트를 초안대로 20sp로 실기기(에뮬레이터)에 렌더링해보니, 원형 화면 상단의 좁은 안전 영역 폭을 넘어서면서 보드와 겹치는 문제가 실제로 발생했다("Black to move"가 화면 상단을 침범). HTML 목업은 작은 삽화였을 뿐 실제 폰트 렌더링 크기와 비례가 달랐다는 뜻 — 13sp로 줄이고, 폭을 화면의 78%로 제한 + 1줄 말줄임(ellipsis) 처리를 추가해 재확인함(6절 스크린샷 참고).
| `buttonLabel` | 14sp / Medium | 버튼 텍스트 |

## 4. 간격/크기 (8dp 기준 그리드, 비율 기반)

퍼즐 화면은 보드 주변 여백이 크다는 피드백을 반영해 **보드를 최대한 키우고, 비워두던 여백에 좌표·기보 입력 같은 실제 기능을 배치**하는 방향으로 조정했다. 보드는 더 이상 별도 프레임(테두리·둥근 모서리)을 갖지 않고 칸 색상 자체가 경계 역할을 한다.

- **체스판 크기 — 내접 정사각형으로 재계산**: 초안(화면 지름의 92%)은 원형 세이프 영역보다 정사각형이 더 커서 네 모서리(a1/a8/h1/h8 칸)가 화면 바깥으로 잘리는 문제가 있었다. 올바른 계산은 **정사각형이 원에 내접하도록(네 모서리가 원 둘레에 닿거나 안쪽에 있도록) 한 변 = 지름 × (1/√2) ≈ 지름의 70.7%**로 잡는 것 — 예: 지름 214px 기준 한 변 ≈148px. 이 값을 기준으로 실기기에서 Horologist round-screen 유틸로 정밀 계산한다.
- **체스판**: 정사각형, 위 내접 크기로 확정. **외곽 테두리(box-shadow 프레임) 없음, 모서리 반경 0** — 칸 자체가 경계다.
- **랭크 번호(좌측)**: 보드 왼쪽에 1열, 보드의 각 행 높이와 동일한 8개 셀에 8→1 숫자 표시(표준 방향: 맨 위 행=8, 맨 아래 행=1). 폭은 숫자 한 글자 기준 최소 폭, `textSecondary`보다 더 낮은 명도의 dim 톤으로 보드 색보다 눈에 덜 띄게.
- **좌표 입력 탭(우측)**: 체스판을 내접 크기로 줄이면서 좌우에 남는 여백이 커졌고, 그 여백을 거의 채우도록 탭을 확대했다(보드 한 변의 약 60%에 해당하는 높이, 폭도 기존 대비 확대) — 작은 알약 대신 여백을 가득 채우는 세로형 버튼에 가깝게. 보드에서 살짝 더 떨어뜨려 여백 쪽으로 이동시켰다. **모양은 그 여백 자체(원형 화면 경계와 보드의 곧은 변 사이 반달 모양)를 따라간다** — 보드와 맞닿는 안쪽 변은 거의 직각(반경 최소), 화면 바깥쪽을 향한 바깥 변은 완전히 둥글게(반원형 캡) 처리해 일반 둥근 사각형이 아니라 반달 실루엣에 가깝게 만든다(Compose 구현 시 `RoundedCornerShape(topStart, topEnd=50%, bottomEnd=50%, bottomStart)`로 대응). `surface` 배경(반투명), 키보드 글리프(⌨) 표시. 탭하면 6절에서 다루는 좌표 입력(예: `e2e4`)용 텍스트 입력이 열린다. 시각적 크기와 별개로 실제 터치 영역은 `Modifier.minimumInteractiveComponentSize()`로 48dp까지 확장(6절 리스크 2 참고).
- **차례 표시(상단)**: 보드보다 위, 보드와 겹치지 않도록 충분한 간격을 두고 배경 없는 텍스트로 "White to move" / "Black to move" 표시.
- **레이팅(하단)**: 보드보다 아래, 보드와 겹치지 않는 위치에 **모서리가 둥근 사각형 칩**으로 감싸 표시(예: `1632`). 정답 시 `+14`처럼 변화량을 성공색으로 옆에 덧붙인다.
- 버튼(로그인 화면): 최소 높이 52dp(Wear 접근성 최소 터치 타깃 48dp 초과), 폭 = 안전영역의 70%, 모서리 반경 26dp(알약형) — 로그인 화면은 보드가 없으므로 기존 버튼형 유지.
- 화면 간 공통 배치: 상태/안내 텍스트는 항상 화면 상단, 주 조작 대상(버튼 또는 보드)은 항상 화면 중앙 — 두 화면 모두 동일한 배치 규칙 적용(DEVELOP.md "같은 기능은 모든 화면에서 같은 자리" 원칙).

## 5. 화면별 상태 설계

모든 온스크린 문구는 한글 없이 **영어로만** 구성한다.

**로그인 화면**
- 기본: 상단 캡션 "LICHESS PUZZLE" + 중앙 "Sign in with Lichess" 버튼(accent 배경).
- 진행 중: 버튼 대신 `CircularProgressIndicator`(accent) + 캡션 "Complete sign-in on your phone".
- 실패: 버튼 유지 + 버튼 아래 `caption` 색상 `error`로 "Sign-in failed. Tap to retry." — 확인창 없이 버튼 재탭으로 즉시 재시도(되돌리기 우선, DEVELOP.md 지침).

**퍼즐 화면**
- 기본(내 차례): 상단 텍스트 "White to move" / "Black to move"(배경 없음) + 좌측 랭크 번호 + 중앙의 확대된 보드 + 우측 좌표 입력 탭 + 하단 레이팅 숫자.
- 로딩(퍼즐 요청 중): 상단 텍스트 "Loading…", 보드 dim 처리(투명도 35%) + `CircularProgressIndicator` 오버레이, 하단은 직전 레이팅 유지.
- 오답: 시도한 목적지 칸만 `error` 색으로 즉시 배경 강조(200ms) → 기물 원위치 애니메이션(300ms), 상단 텍스트 일시적으로 "Try again"(error 색) → 원래 차례 텍스트로 복귀. 보드 전체 테두리는 더 이상 사용하지 않음(4절 — 보드에 프레임이 없어짐).
- 정답(중간 수): 방금 둔 출발/도착 두 칸에 `lastMoveOutline` 강조(칸 안쪽 accent 테두리) → 상대 응수 자동 재생(600ms 애니메이션) → 상단 텍스트가 다음 차례로 갱신.
- 정답(퍼즐 완료): 상단 텍스트 "Correct"(success 색) 0.8초 표시 → 하단 레이팅 칩이 갱신된 값 + `+14` 같은 변화량으로 즉시 갱신 → 자동으로 다음 퍼즐 로딩 상태로 전환(사용자 추가 조작 없이, "화면을 옮기지 않고 끝낼 수 있는 작업은 옮기지 않는다" 원칙).
- 네트워크 오류(블루투스 프록시 끊김 등): 상단 텍스트 `error` 색으로 "Connection Lost" — **이 텍스트 자체가 탭 가능한 재시도 버튼**을 겸한다(별도 "Retry" 요소를 추가하지 않음). 하단 레이팅 칩은 다른 상태와 동일하게 마지막으로 알고 있던 값을 계속 표시(연결이 끊겼다고 레이팅 정보까지 가릴 필요는 없음).
- **좌표 입력**: 우측 탭(⌨) 탭 → `MoveInputLauncher`가 워치 시스템 키보드를 전체화면으로 실행(RESEARCH.md 10-A절) → 입력 결과를 보드 하단 위에 작은 모노스페이스 텍스트 상자로 에코(예: `Nc3`, 커서 깜빡임) → 형식이 올바르면 일반 탭-이동과 동일하게 처리, 형식이 잘못됐거나 비합법수면 오답과 동일한 피드백. **입력 표기법은 UCI(`e2e4`)가 아니라 표준 대수 기보법(SAN, 예: `Nc3`, `exd5`, `O-O`)** — chesslib의 SAN 파싱/생성 기능을 그대로 사용(RESEARCH.md 8절). 이 상태에서도 하단 레이팅 칩은 계속 표시한다(연결 끊김 상태와 동일하게, 입력 중이라고 다른 정보를 가릴 필요는 없음).

## 6. 접근성

- 모든 텍스트/배경 조합은 WCAG AA 이상 대비(위 표에 대비비 명시). 기물은 칸 색과 무관하게 항상 반대색 1.5dp 외곽선을 둘러 가독성 확보.
- 버튼 최소 터치 타깃 52dp(48dp 기준 초과 충족).
- **알려진 리스크 1 — 보드 칸 터치 타깃**: 8×8 보드를 원형 480px 화면에 내접시키면(4절, 보드를 최대한 키운 뒤에도) 칸 하나당 변 길이가 Wear 접근성 권장 최소 터치 타깃(48dp)에 못 미칠 가능성이 높음(체스가 8칸 고정이라 대안이 없음). **완화책**: 시각적 칸 경계보다 히트박스를 사방 각 3~4dp 더 넓게 잡아 인접 칸 오탭 확률을 낮추고, 탭 시 해당 칸 전체를 `selectedSquare` 색으로 크게 하이라이트해 사용자가 인식/정정하기 쉽게 한다. 드래그 대신 탭-선택 방식을 쓴 이유이기도 하다(RESEARCH.md 9절).
- **알려진 리스크 2 — 좌표 입력 탭 크기**: 우측 좌표 입력 탭은 시각적 폭이 48dp에 크게 못 미친다(보드 옆 여백에 맞춘 좁은 탭). **완화책**: 시각적 크기와 무관하게 Wear Compose의 `Modifier.minimumInteractiveComponentSize()`로 실제 터치 영역을 48dp까지 확장(다른 요소와 겹치지 않는 범위에서) — 시각적으로는 얇게, 터치 판정은 넓게 유지한다.

## 7. 디자인 기술 스택

- 디자인 도구: 별도 Figma 파일 없이 **Claude가 직접 값 설계 + HTML 목업으로 시각 확인**(2화면짜리 개인용 앱에 Figma 왕복은 비용 대비 이득이 낮다고 판단 — 대안으로 기록).
- 서체: 시스템 기본(Wear OS 기본 Roboto Flex 계열), 무료, 별도 라이선스 확인 불필요.
- 아이콘(기물): 유니코드 체스 글리프 중 **한 가지 실루엣 세트(♟♞♝♜♛♚)만 사용**하고, 색은 글리프 자체(흰/검 전용 코드포인트를 섞어 쓰기)가 아니라 우리 쪽 채우기/외곽선 색으로만 구분한다 — 양 진영이 같은 모양 언어를 쓰도록 통일하기 위함. 추가 에셋/라이선스 불필요. 렌더링 폭이 기기에서 부족하면 Wear Compose Material Symbols(Google, Apache 2.0, 무료)로 대체.
- 차트: 해당 없음(범위 밖).
- 토큰 전달 경로(단일 소스): 이 문서의 값 → 구현 시 `ui/theme/Color.kt`, `Type.kt`, `Dimens.kt` (Kotlin object 상수)로 직접 옮겨 적음. 외부 동기화 도구 불필요.
- 구현 비용이 큰 요소와 대안: 커스텀 폰트·별도 SVG 기물 세트는 채택하지 않고 시스템 폰트/유니코드 글리프로 대체(위 서체/아이콘 항목에 근거 기록).
- 좌표 입력: 별도 UI 프레임워크 없이 `androidx.wear:wear-input`의 `RemoteInputIntentHelper`로 워치 시스템 입력 선택기(키보드/딕테이션/손글씨)를 그대로 사용 — 커스텀 키패드를 직접 그리지 않음(RESEARCH.md 10-A절, PLAN.md 6절 트레이드오프 참고).

## 8. 산출물

아래 HTML 목업(로그인/퍼즐 화면 + 로딩/오답/정답/오류 상태)을 Artifact로 게시함: https://claude.ai/code/artifact/bd94fc50-0e8f-4b04-82c4-21da4f42cc62
