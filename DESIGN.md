# DESIGN.md

PLAN.md의 화면 범위(로그인 화면, 퍼즐 화면 2개)에 대한 디자인 확정 문서.

## 1. 개요 및 의도

- 대상: 갤럭시 워치 8, 1.47" 원형 AMOLED, 480×480px.
- 느낌: 체스판 자체는 클래식하고 차분한 원목톤(가독성 우선)으로, 그 외 앱 크롬(배경·버튼·상태 텍스트)은 따뜻한 뉴트럴 톤 + 소량의 클레이(terracotta) 포인트 컬러로 통일해 "Claude" 디자인 언어의 절제되고 따뜻한 인상을 준다.
- OLED 워치이므로 배경은 순검정에 가깝게 유지해 소비전력과 대비를 동시에 확보한다.
- 화면마다 목적 하나: 로그인 화면은 "로그인"만, 퍼즐 화면은 "지금 둘 수를 파악하고 두는 것"만 최상단에 크게 배치.

## 2. 색상 (Hex)

**앱 크롬**
| 토큰 | 값 | 용도 |
|---|---|---|
| `background` | `#000000` | 화면 배경(OLED) |
| `surface` | `#1C1B1A` | 상태 칩/오버레이 배경 |
| `surfaceAlpha85` | `rgba(28,27,26,0.85)` | 보드 위에 얹는 상태 칩(반투명) |
| `textPrimary` | `#F5F1EA` | 주요 텍스트(대비 배경 대비 ≈18.6:1) |
| `textSecondary` | `#A69C8D` | 보조 텍스트(레이팅 등, 대비 ≈7:1) |
| `accent` | `#C15F3C` | 기본 버튼, 선택 표시 |
| `success` | `#4C9A6A` | 정답 피드백 |
| `error` | `#D14343` | 오답/네트워크 오류 피드백 |
| `ratingUp` | `#FF5C5C` | 레이팅 상승분(밝은 빨강) |
| `ratingDown` | `#4FA8FF` | 레이팅 하락분(밝은 파랑) |

> **구현 중 수정(레이팅 증감 색)**: 처음엔 상승만 `success`(녹색)로 표시했는데, 사용자 요청으로 **한국 주식 시장 관례(상승=빨강, 하락=파랑)**를 따르도록 변경 — 상승분은 `ratingUp`(밝은 빨강), 하락분은 `ratingDown`(밝은 파랑)으로 델타 숫자만 색을 입힌다(레이팅 본 숫자는 `textSecondary` 그대로). 괄호도 없앰 — 아래 5절 참고.

**체스판**

> **구현 중 수정 1(사용자 제공 참고 이미지 반영)**: 원목톤(`#EDEAE1`/`#7C6A58`) 대신 사용자가 공유한 참고 이미지에 맞춰 **한색 계열 파란 보드**로 교체. 기물도 얇은 유니코드 글리프 대신 **둥글고 통통한 아이콘 스타일**로 새로 그렸다 — 그 시점엔 참고 이미지가 정확히 어떤 라이선스의 에셋인지 확인할 수 없어 트레이싱하지 않고, 같은 분위기를 내는 독자적인 벡터 그림으로 새로 그렸음(DEVELOP.md 디자인 기술 스택 라이선스 확인 항목).
>
> **구현 중 수정 2(실제 에셋 이미지로 교체)**: 이후 사용자가 라이선스 문제없이 쓸 수 있는 실제 기물 PNG 세트를 직접 제공(`LS/`=밝은 칸 배경 위, `DS/`=어두운 칸 배경 위, 파일명 `[W|B][P|N|B|R|Q|K].png`, 예: `WK.png`=흰 킹, `BQ.png`=검은 퀸, `BG.png`=빈 칸). 각 PNG는 칸 배경과 기물이 이미 하나로 합성된 101×101 불투명 이미지라, 수정 1의 벡터 그림을 걷어내고 이 이미지에서 뽑은 기물 그림으로 교체했다. 다만 배경까지 합쳐진 그대로 칸마다 통째로 그리면 이후 "기물이 칸 사이를 슬라이드 이동하는" 애니메이션을 넣을 때 기물 밑에 깔린 배경 사각형까지 함께 움직여 버려 자연스러운 이동 표현이 어렵다(사용자와 확인한 트레이드오프). 그래서:
> 1. 두 세트 모두 기물 그림 자체(형태·음영)는 동일하고 배경만 다르다는 걸 확인.
> 2. 색상 대비가 더 큰 쪽에서 골라 잘라내면 경계가 깨끗하다 — **흰 기물은 `DS`(파란 배경)에서, 검은 기물은 `LS`(옅은 배경)에서** 알파(투명) 마스크로 추출.
> 3. 경계의 anti-aliased 픽셀은 그 배경색을 알고 있으므로 색 오염 제거(un-blend/decontaminate) 처리해, 다른 색 칸 위에 올려도 배경색이 번져 보이지 않게 함.
> 4. 잘라낸 기물 12장(흑/백 × 6종)만 `app/src/main/res/drawable-nodpi/piece_{w|b}{p|n|b|r|q|k}.png`로 앱에 포함 — 칸 배경은 이 이미지에서 뽑은 단색(`boardLight`/`boardDark`, 아래 표)으로 코드에서 그대로 그린다. `PieceIcons.kt`는 벡터 path 대신 이 비트맵을 그리는 `Image` 컴포저블로 교체됨. 원본 26장(`LS/`, `DS/`)은 저장소에 참고용으로 남겨두되 앱 리소스로는 쓰지 않음.
>
> **구현 중 수정 3(테두리를 살짝 더 두껍게)**: 기물은 이제 색 토큰이 아니라 비트맵이라, 외곽선 굵기도 코드가 아니라 `tools/extract_pieces.py`의 추출 단계에서 조절한다. 추출 전 원본에 1px 침식 필터(`ImageFilter.MinFilter(3)`)를 30% 세기로만 블렌드해 어두운 선(외곽선·세부 디테일)을 아주 살짝만 굵혀 잘라낸다 — 100% 세기(완전한 1px 팽창)는 눈에 띄게 두꺼워져 "약간만"이라는 요청과 맞지 않아 낮췄다.
>
> **구현 중 수정 4(테두리가 울퉁불퉁해 보임 — 에셋 자체를 실제 표시 크기로 축소)**: 기물이 원본 해상도(~90~100px) 그대로 앱에 들어가 있어, 보드 한 칸(실기기에서 약 30~40px) 크기로 그려질 때 2.5배 이상 축소돼 외곽선이 계단처럼 보였다. `PieceIcons.kt`에서 `FilterQuality.High`를 요청해봤지만 실측 결과 스크린샷이 픽셀 단위로 동일해 효과가 전혀 없었다(이 에뮬레이터/Skia 조합에서 축소 시 필터 품질 힌트가 실질적으로 적용되지 않는 것으로 보임 — GPU 이중선형 축소는 2×2 텍셀만 샘플링해 큰 배율의 축소에서는 원래 잘 앤티에일리어싱되지 않는다). 대신 `tools/extract_pieces.py`에 최종 단계로 **모든 기물에 동일한 배율(0.5배)의 다운스케일**을 추가해, 실제 표시 크기에 가깝게(약 30~48px) 미리 축소한 에셋을 만든다 — 축소 필터는 PIL의 LANCZOS를 쓰되, 알파를 미리 곱한(premultiplied) 상태로 리사이즈한 뒤 다시 나눠(un-premultiply) 저장해, 완전 투명 픽셀(RGB를 0으로 채워둔 픽셀)의 검은색이 반투명 경계에 번져 보이는 것을 방지했다. 모든 기물에 같은 배율을 적용해 기물 간 상대적 크기 비율은 그대로 유지된다. (효과 없던 `FilterQuality.High`는 최종 코드 정리 때 제거 — 아래 수정 8.)
>
> **구현 중 수정 5(테두리 바깥쪽에 옅은 흰색 번짐)**: 흰 기물(파란 `DS` 배경에서 추출) 외곽선 바로 바깥에 거의 안 보일 만큼 옅지만 실제로는 순백(`#FFFFFF`)인 픽셀 고리가 남아, 파란 어두운 칸 위에서 흐릿한 흰 테두리처럼 도드라졌다. 처음엔 "거의 0에 가까운 알파로 나눠서 원래 색을 복원(decontaminate)"하는 계산이 알파가 아주 작을 때 불안정해져 흰색으로 튄다고 보고, `ALPHA_FLOOR` 미만 알파 픽셀을 그냥 투명으로 스냅하는 식으로 두 군데(`extract()`, `resize_straight_alpha()`)를 땜질했다.
>
> **구현 중 수정 6(위 문제의 근본 수정 — 바깥쪽은 애초에 `#000000` 고정, 알파만 사용)**: 관찰해보면 기물의 어두운 외곽선 스트로크가 실루엣 전체를 한 바퀴 둘러싸고 있어서, 배경과 맞닿는 픽셀은 **항상** 이 외곽선을 통해서만 배경과 섞인다(밝은 채우기색이 배경과 직접 맞닿는 지점은 없음). `extract()`를 다시 써서 부분 알파 구간은 색을 `(0,0,0)`으로 고정하고 알파만 거리 기반으로 계산하도록 했다 — 배경색을 나눠서 복원하는 코드 자체를 없앴다.
>
> **구현 중 수정 7(수정 6이 실제로는 안 고쳐짐 — 거리 임계값이 완전히 잘못 잡혀 있었음)**: 수정 6을 실기기에서 확인해보니 여전히 남아있었다. 원인을 다시 보니 수정 6의 "부분 알파 구간"을 가르는 거리 임계값(`LOW`~`HIGH` = 16~42)이 옛날 decontaminate 방식에 맞춰 잡힌 값이라, 실제 anti-aliasing이 배경↔외곽선 사이에서 만들어내는 색 거리 범위(파란 `DS` 배경 기준 최대 약 300 단위)에 비하면 턱없이 좁았다. 그 결과 절반 이상 섞인 회색·옅은 픽셀 대부분이 "완전 불투명, 원본 색 그대로"로 잘못 분류돼 — 배경도 아니고 순수 외곽선도 아닌 흐릿한 회색/흰색이 **알파 100%로 고정**된 채 고리 모양으로 남아있었다(옅은 파랑에 가까운 회색은 흰 기물의 실제 채우기색과 색 거리가 비슷해, 색 거리만으로는 "이건 진짜 채우기다"와 "이건 절반 섞인 잔상이다"를 구분할 수 없다는 게 근본 문제).
>
> 색만으로는 구분이 안 되니 **위상(topology)으로 구분**하도록 다시 짰다: 1) 픽셀 밝기가 배경 밝기의 60% 미만이면 "벽(외곽선)"으로 표시. 2) 캔버스 테두리에서 시작해 벽을 통과하지 않고 갈 수 있는 모든 칸을 BFS로 채운다(`flood_fill_exterior`) — 도달한 칸은 전부 "바깥쪽"(배경 또는 그 언저리의 번짐), 벽에 막혀 도달 못한 칸은 전부 "안쪽"(외곽선 자체 또는 그 안에 진짜로 둘러싸인 채우기색)이다. 실루엣이 끊김 없이 닫혀 있는 한 이 판정은 색이 얼마나 밝든 어둡든 정확하다. 바깥쪽으로 분류된 픽셀만 배경 근접 시 투명, 그 외엔 검정+거리기반 알파로 처리하고, 안쪽으로 분류된 픽셀은 원본 색을 알파 100%로 그대로 쓴다.
>
> **구현 중 수정 8(최종 확정 — 원본/추출 스크립트 삭제 + 코드 정리)**: 수정 7까지 실기기로 확인해 문제없음을 확인한 뒤, 사용자가 이 결과를 최종본으로 확정하고 더 이상 필요 없는 중간 산출물을 정리해달라고 요청했다. `LS/`·`DS/` 원본 26장과 `tools/extract_pieces.py` 추출 스크립트를 저장소에서 삭제했다 — **이제 `res/drawable-nodpi/piece_*.png` 12장 자체가 단일 소스**이며, 이 문서에 기록된 추출 알고리즘(배경색 채취 → 외곽선 굵기 조정 → 위상 기반 알파 마스크 → 0.5배 LANCZOS 축소)은 그 결과물이 왜 지금 모습인지 설명하는 기록으로만 남는다. 원본 이미지나 다른 배율로 다시 뽑아야 하면 커밋 `7ac943e`(삭제 직전)에서 `LS/`, `DS/`, `tools/extract_pieces.py`를 되살리면 된다. 같은 정리 작업으로 `PieceIcons.kt`도 손봤다 — 위 수정 4에서 넣은 `FilterQuality.High`(효과가 실측상 없었던)와 그걸 위해 끌어온 `ImageBitmap`/`imageResource` 경로를 걷어내고, 원래의 더 단순한 `painterResource` 기반 `Image()` 호출로 되돌렸다. 실제 개선은 전부 에셋 자체(수정 4·6·7)에서 끝나 있어 런타임 쪽에 남겨둘 이유가 없었다.

| 토큰 | 값 | 용도 |
|---|---|---|
| `boardLight` | `#EDF3FA` | 밝은 칸(`LS/BG.png`에서 채취) |
| `boardDark` | `#5A99F2` | 어두운 칸(`DS/BG.png`에서 채취) |
| (기물 채우기/외곽선) | — | 더 이상 색 토큰이 아니라 `piece_*.png` 비트맵 자체에 포함(위 수정 2) |
| `selectedSquare` | `#A4C6F6` (boardLight/boardDark RGB 평균, 코드에서 계산) | 선택한 기물의 칸 |
| `legalDot` | `rgba(0,0,0,0.25)` | 이동 가능한 칸(작은 점) / 상대 기물을 잡을 수 있는 칸(칸에 내접하는 원형 테두리, 같은 색) |
| `lastMoveOutline` | `rgba(193,95,60,0.9)`, 2dp | 방금 둔 수 강조(미구현) |
| `hintTint` | `rgba(255,255,0,0.375)` | 힌트로 지목된 칸(사용자 요청 — 아래 5절 참고) |

> **구현 중 수정(선택 칸·이동 표시 — 사용자 요청)**: 선택 칸은 accent 반투명 틴트 대신 **`boardLight`/`boardDark`의 RGB 채널별 평균값을 그대로 배경색으로** 써서(완전 불투명), 어느 색 칸이든 같은 중립 톤으로 강조되게 했다(`Color.kt`에서 두 토큰으로부터 계산 — 하드코딩 안 함, 보드 색이 바뀌면 자동으로 따라감). 선택 시 이동 가능한 칸도 함께 표시한다: 빈 칸으로 이동은 칸 중앙의 작은 원(`legalDot`, 지름 ≈ 칸의 30%), 상대 기물을 잡는 이동은 칸에 내접하는 원을 기물 **뒤에** 그려 살짝 어두운 테두리처럼 보이게 한다 — 둘 다 같은 `legalDot` 색. 합법수 목록은 `Board.legalMoves()`(chesslib)를 선택된 칸으로 필터링해 구한다 — 퍼즐의 정답 하나만이 아니라 실제 합법수 전부를 보여준다(사용자가 명시적으로 요청한 표준 체스 UI 관례).
>
> **구현 중 수정(캡처 원을 테두리만 남기고 속 비우기 — 사용자 요청)**: 처음엔 캡처 칸도 꽉 찬 원으로 그렸는데, 안쪽을 비우고 테두리만 보이게 해달라는 요청에 `Stroke`로 바꿨다. 테두리 굵기는 처음엔 작은 원(빈 칸용)의 반지름과 같게(`size.minDimension * 0.15f`) 맞췄다가, 너무 두꺼워 보인다는 후속 피드백에 그 **2/3로 다시 줄였다**(`size.minDimension * 0.15f * 2/3`). 원 자체의 반지름은 항상 `내접 반지름 - 테두리 굵기/2`로 계산해 **바깥쪽 가장자리가 꽉 찬 원이었을 때와 같은 자리**(칸에 내접)에 오도록 한다 — 겉보기 크기는 그대로 두고 속만 비운 셈이다.

## 3. 타이포그래피

시스템 기본 서체(Wear OS 기본, Roboto Flex 계열) 그대로 사용 — 커스텀 폰트 임베딩은 2화면짜리 개인 앱에는 비용 대비 이득이 낮아 채택하지 않음(대안 기록).

| 토큰 | 크기/굵기 | 용도 |
|---|---|---|
| `turnLabel` | 13sp / SemiBold (초안 20sp에서 축소 — 아래 참고) | "Correct", "White to move" 등 상단 텍스트 |
| `bodyRating` | 11sp / Regular (초안 14sp에서 축소) | 현재 퍼즐 레이팅 — 보드보다 아래로 더 내려 배치(4절), 부제 성격이라 `caption`과 같은 크기로 축소 |
| `caption` | 11sp / Regular | 부가 안내(에러 상세 등) |

> **구현 중 수정**: 상단 텍스트를 초안대로 20sp로 실기기(에뮬레이터)에 렌더링해보니, 원형 화면 상단의 좁은 안전 영역 폭을 넘어서면서 보드와 겹치는 문제가 실제로 발생했다("Black to move"가 화면 상단을 침범). HTML 목업은 작은 삽화였을 뿐 실제 폰트 렌더링 크기와 비례가 달랐다는 뜻 — 13sp로 줄이고, 폭을 화면의 78%로 제한 + 1줄 말줄임(ellipsis) 처리를 추가해 재확인함(6절 스크린샷 참고).
| `buttonLabel` | 14sp / Medium | 버튼 텍스트 |

## 4. 간격/크기 (8dp 기준 그리드, 비율 기반)

퍼즐 화면은 보드 주변 여백이 크다는 피드백을 반영해 **보드를 최대한 키우고, 비워두던 여백에 좌표·기보 입력 같은 실제 기능을 배치**하는 방향으로 조정했다. 보드는 더 이상 별도 프레임(테두리·둥근 모서리)을 갖지 않고 칸 색상 자체가 경계 역할을 한다.

- **체스판 크기 — 내접 정사각형으로 재계산**: 초안(화면 지름의 92%)은 원형 세이프 영역보다 정사각형이 더 커서 네 모서리(a1/a8/h1/h8 칸)가 화면 바깥으로 잘리는 문제가 있었다. 산술적으로 올바른 계산은 **정사각형이 원에 내접하도록(네 모서리가 원 둘레에 닿거나 안쪽에 있도록) 한 변 = 지름 × (1/√2) ≈ 지름의 70.7%**로 잡는 것이다. 이 값은 보드 그 자체의 네 모서리만 안전할 뿐 — **보드 왼쪽 바깥에 랭크 번호 열이 추가로 붙기 때문에, 보드가 딱 내접 크기(70.7%)일 때는 랭크 번호가 설 자리가 전혀 없다**(보드 자체 모서리가 이미 원 둘레에 닿아 있어서). 실기기(에뮬레이터) 스크린샷에서 랭크 8/1 숫자가 화면 바깥으로 잘리는 걸 실제로 확인. 한때 지름의 64%로 줄여 랭크 번호 열 여백을 확보했었으나, **보드 자체를 최대화하라는 명시적 지시에 따라 다시 70.7%(내접 정사각형)로 확정** — 랭크 번호 열이 화면 밖으로 일부 잘리는 것은 알고 받아들인 트레이드오프다.
- **체스판**: 정사각형, 위 내접 크기(지름의 70.7%, 1/√2)로 확정. **외곽 테두리(box-shadow 프레임) 없음, 모서리 반경 0** — 칸 자체가 경계다.
- **보드 방향**: 흑 차례일 때는 보드를 180도 돌려 표시한다(사용자 요청) — 자신의 기물이 항상 화면 아래쪽에 오도록, 대부분의 체스 UI와 동일한 관례. `Board()`에서 실제 파일/랭크 ↔ Square 매핑은 그대로 두고, 화면에 그릴 때 어느 (파일,랭크)를 어느 화면 칸에 배치할지만 뒤집는다(`displayRow`/`displayCol` → 흑 차례면 `7-`). 칸 색(`isLight`)은 뒤집기 전의 실제 파일/랭크로 계산해야 한다 — 화면 위치가 아니라 칸 자체의 색은 보는 방향과 무관하게 항상 같아야 하기 때문(a1은 항상 어두운 칸).
- **힌트 탭(좌측, 사용자 요청 — 랭크 번호 대체)**: 기존 랭크 번호(8→1) 열을 없애고 같은 자리에 **힌트 버튼**을 배치했다. 크기·모양은 우측 좌표 입력 탭과 "동일한 양식"(사용자 표현 그대로) — 같은 `Dimens.KeyboardTabWidth`/`KeyboardTabHeightRatio`를 그대로 재사용하고, 모양은 좌표 입력 탭의 반달 모양(`HalfMoonShape`)을 좌우로 정확히 뒤집은 `HintTabShape`(곡선이 왼쪽 화면 바깥쪽을 향함, 보드와 맞닿는 오른쪽은 작은 모서리)를 새로 그렸다. 아이콘은 전구. 두 탭이 이제 폭·여백까지 완전히 대칭이라 아래 "가로 위치 보정"이 통째로 필요 없어졌다(보드가 저절로 중앙에 옴) — `PuzzleScreen.kt`의 `groupShift` 계산을 제거함.
  > **구현 중 수정(사용자 요청 — 이모지 대신 벡터 아이콘)**: 처음엔 전구 이모지(💡)를 그대로 썼는데, 이모지는 폰트가 정해둔 자기만의 색(노랑/파랑)으로만 렌더링돼 `color =` 파라미터를 무시한다 — 옆의 키보드 탭 글리프(⌨, 단색 accent 틴트)와 느낌이 안 맞는다는 피드백. Material Design의 표준 "lightbulb" 아이콘 path(Google, Apache 2.0)를 `res/drawable/ic_hint.xml` 벡터로 새로 넣고, `ColorFilter.tint(Accent)`로 칠해 키보드 글리프와 동일하게 단색·틴트 가능한 모양으로 통일했다.
  - **동작**: 첫 탭 → 이번에 움직여야 할 기물의 칸에 `hintTint`(`rgba(255,255,0,0.375)`) 색을 씌워 표시(보드는 건드리지 않음). 같은 칸이 표시된 상태에서 다시 탭 → 그 기물을 실제로 이동시킨다(정답 처리와 동일 경로 — 상대 자동 응수, 퍼즐 완료 처리까지 전부 재사용). 힌트로 표시된 정답 수는 `PuzzleEngine.hintMove`(신규, `puzzle.solution[solutionIndex]`를 보드는 안 건드리고 `Move`로만 파싱)에서 가져온다 — **리체스 퍼즐 API 응답에 "힌트"라는 별도 필드는 없고, `solution`(정답 수순 배열) 자체가 힌트 데이터의 전부다**(`core/lichess/LichessModels.kt`의 `Puzzle` 확인). 보드 칸을 직접 탭하거나 키보드로 SAN을 입력하면 힌트 표시는 취소된다(사용자가 수동 조작을 시작했다는 뜻이므로).
    > **구현 중 수정 네 차례(표시 방식·색·투명도 — 사용자 요청)**: 처음엔 얇은 accent 테두리만 그렸는데, 잘 안 보인다는 피드백에 실제 탭-선택과 완전히 동일하게(`selectedSquare` 배경 + 이동 가능 점/원까지) 바꿨다. 이후 "그렇게 말고, 진짜 선택한 것처럼 보이지 않게 해달라"는 정반대 방향의 후속 요청에 다시 바꿔, **칸 배경과 기물 사이에 반투명 색을 한 겹 씌우는 것**으로 정했다(처음엔 빨강 `#FF000080`) — 이동 가능 점/원은 안 뜨고, 테두리도 없다. Compose 레이어 순서상 `Box`에 `.background()`를 두 번 연달아 쓰면(칸 고유색 → `hintTint`) 뒤에 그려지는 배경이 앞의 배경 위에 겹쳐지지만, `Box`의 실제 자식(기물 아이콘)보다는 항상 아래에 그려지는 걸 이용했다 — "배경과 기물 사이"라는 요청이 별도 레이어 없이 배경 색을 두 번 칠하는 것만으로 그대로 구현된다. 이후 색을 빨강 → 노랑으로 바꾸고, 투명도를 50% → 25% → 37.5%로 세 차례에 걸쳐 각각 별도 요청에 맞춰 `Color.kt`의 `HintTint` 값만 교체했다.
- **좌표 입력 탭(우측)**: 체스판을 내접 크기로 줄이면서 좌우에 남는 여백이 커졌고, 그 여백을 거의 채우도록 탭을 확대했다(보드 한 변의 약 52%에 해당하는 높이, 폭 17dp) — 작은 알약 대신 여백을 채우는 세로형 버튼에 가깝게. 보드에서 살짝 더 떨어뜨려 여백 쪽으로 이동시켰다. **모양은 그 여백 자체(원형 화면 경계와 보드의 곧은 변 사이 반달 모양)를 따라간다** — 보드와 맞닿는 안쪽 변은 거의 직각(반경 최소), 화면 바깥쪽을 향한 바깥 변은 완전히 둥글게(반원형 캡) 처리해 일반 둥근 사각형이 아니라 반달 실루엣에 가깝게 만든다. `surface` 배경(반투명), 키보드 글리프(⌨, `caption` 11sp — 기본 텍스트 크기로는 좁은 탭 안에서 커 보인다는 피드백에 축소) 표시. 탭하면 6절에서 다루는 좌표 입력(예: `Nc3`)용 텍스트 입력이 열린다. 시각적 크기와 별개로 실제 터치 영역은 `Modifier.minimumInteractiveComponentSize()`로 48dp까지 확장(6절 리스크 2 참고).
  > **구현 중 수정 1**: 처음엔 `RoundedCornerShape(topEnd = 50%, ...)`로 구현했는데, 실기기(에뮬레이터) 스크린샷을 보니 오른쪽 가운데에 직선이 그대로 남아 있었다. Compose의 `RoundedCornerShape`는 모서리마다 원형(circular) 반경 하나만 가지며, 그 반경이 짧은 인접 변(여기선 22dp 폭)에 맞춰 강제로 잘리기 때문에 큰 값을 줘도 실제로는 거의 반영되지 않았다 — CSS의 `border-radius: 가로 / 세로`처럼 가로·세로를 따로 지정하는 기능이 Compose 기본 API엔 없다. 오른쪽을 (가로반경=폭/2, 세로반경=높이/2)인 타원의 절반으로 직접 그리는 커스텀 `Shape`(`HalfMoonShape`, `PuzzleScreen.kt`)로 교체해 해결했다.
  > **구현 중 수정 2(사용자 요청 — 크기 축소 + 8절 목업과 형태 일치)**: 탭이 너무 크다는 피드백에 폭 22dp→17dp, 높이 비율 0.62→0.52로 줄였다. 형태도 8절 HTML 목업의 `.kbd-tab` CSS(`border-radius: 3px 19px 19px 3px / 3px 46px 46px 3px`, 22×92 박스)와 다시 대조해보니, 수정 1의 진짜 반타원(rx=w/2, ry=h/2)은 왼쪽의 3dp 모서리 이후 `lineTo(w/2, 0)`로 평평한 구간을 한 번 더 거쳐서야 곡선이 시작돼 목업과 미세하게 달랐다 — 목업 CSS는 모서리 반경(3+19=22=박스 폭, 46+46=92=박스 높이)이 정확히 박스 크기와 일치해 평평한 구간이 전혀 남지 않는다. `HalfMoonShape`의 오른쪽 타원 반경을 `rx = w - leftCornerRadius`(목업의 19px에 해당)로 바꿔 동일한 무(無)평탄 구간 곡선을 재현함 — 폭/높이 값이 달라도 항상 왼쪽 모서리가 끝나는 지점부터 곧장 곡선이 시작된다.
- **앱 아이콘**: Lichess 나이트 마크(2절)의 귀 끝이 실기기 원형 아이콘 마스크에 잘려 보인다는 피드백 — 처음엔 108dp 캔버스 안에 72dp로 채웠는데(공식 최소 세이프존 66dp보다도 큰 값), 실제로는 도형의 뾰족한 부분이 사각 세이프존 코너 쪽으로 나가 있어 원형 마스크와 여유가 3dp 안팎밖에 없었다. 60dp로 더 줄여(스케일 1.44→1.2, 오프셋 18→24) 여유를 확실히 늘렸다(`ic_launcher_foreground.xml`).
- **화면 유지 시간**: `MainActivity.onCreate()`에서 `FLAG_KEEP_SCREEN_ON`을 켜고 3분 뒤 `Handler.postDelayed`로 해제 — 사용자 요청. 퍼즐을 생각하는 동안 워치 기본 대기시간보다 화면이 먼저 꺼지지 않게 하되, 영구히 켜두지는 않는다.
- **차례 표시(상단)**: 보드보다 위, 보드와 겹치지 않도록 충분한 간격을 두고 배경 없는 텍스트로 "White to move" / "Black to move" 표시.
- **레이팅(하단)**: 보드보다 아래, 보드와 겹치지 않는 위치에 **모서리가 둥근 사각형 칩**으로 감싸 표시(예: `1632`). 정답 시 `+14`처럼 변화량을 델타 색(위 2절 `ratingUp`/`ratingDown`)으로 괄호 없이 옆에 덧붙인다(레이팅 숫자 자체는 `textSecondary` 유지). **구현 중 수정**: 칩을 더 작게(3절 `bodyRating` 11sp로 축소) + 보드에서 더 멀리 하단으로(바닥 여백 9dp→4dp) 옮겨 화면 하단에 덜 무게감 있게 배치. 이후 두 차례 피드백을 더 반영해 바닥 여백을 4dp→7dp→11dp로 점점 더 올림.
- 버튼(로그인 화면): 최소 높이 52dp(Wear 접근성 최소 터치 타깃 48dp 초과), 폭 = 안전영역의 70%, 모서리 반경 26dp(알약형) — 로그인 화면은 보드가 없으므로 기존 버튼형 유지.
- 화면 간 공통 배치: 상태/안내 텍스트는 항상 화면 상단, 주 조작 대상(버튼 또는 보드)은 항상 화면 중앙 — 두 화면 모두 동일한 배치 규칙 적용(DEVELOP.md "같은 기능은 모든 화면에서 같은 자리" 원칙).

## 5. 화면별 상태 설계

모든 온스크린 문구는 한글 없이 **영어로만** 구성한다.

**로그인 화면**
- 기본: 상단 캡션 "LICHESS PUZZLE" + 중앙 "Sign in with Lichess" 버튼(accent 배경).
- 진행 중: 버튼 대신 `CircularProgressIndicator`(accent) + 캡션 "Complete sign-in on your phone".
- 실패: 버튼 유지 + 버튼 아래 `caption` 색상 `error`로 "Sign-in failed. Tap to retry." — 확인창 없이 버튼 재탭으로 즉시 재시도(되돌리기 우선, DEVELOP.md 지침).

**퍼즐 화면**
- 기본(내 차례): 상단 텍스트 "White to move" / "Black to move"(배경 없음) + 좌측 힌트 탭 + 중앙의 확대된 보드(흑 차례면 180도 회전) + 우측 좌표 입력 탭 + 하단 레이팅 숫자.
- 로딩(퍼즐 요청 중): 상단 텍스트 "Loading…", 보드 dim 처리(투명도 35%) + `CircularProgressIndicator` 오버레이, 하단은 직전 레이팅 유지.
- 오답: 시도한 목적지 칸만 `error` 색으로 즉시 배경 강조(200ms) → 기물 원위치 애니메이션(300ms), 상단 텍스트 일시적으로 "Try again"(error 색) → 원래 차례 텍스트로 복귀. 보드 전체 테두리는 더 이상 사용하지 않음(4절 — 보드에 프레임이 없어짐). **"Try again"은 합법수인데 정답이 아닐 때만** 뜬다 — 애초에 그 기물이 갈 수 없는 칸으로 옮기려 한 시도는 "오답"이 아니라 그냥 취소로 취급해 조용히 선택만 풀린다(사용자 요청, 아래 버그 수정 참고).
  > **버그 수정(사용자 제보 — "기물 이동이 안 되는 것 같다")**: 실기기 테스트로 재현해보니, 나이트를 규칙상 갈 수 없는 빈 칸(예: c6→c5, 나이트 모양이 아님)으로 탭/드래그하면 **"Try again"이 잘못 떴다** — 원인은 `PuzzleEngine.attemptCoordinates`가 좌표 하나로 만든 `Move`를 chesslib의 `board.doMove(Move, fullValidation=true)`에 그대로 맡겼는데, 이 검증이 "그 칸에 자기 기물이 있는지"와 "그 수를 두면 자기 킹이 체크당하는지"는 걸러내면서도 **"그 기물이 애초에 그런 모양으로 움직일 수 있는지"는 검증하지 않는다는** 게 실제로 드러났다(기존 코드 주석에 이미 "2-인자 doMove가 자기 편 기물 순서를 무시하고 움직인 적이 있었다"는 비슷한 과거 사례가 남아있었는데, 이번 건 그 수정 이후에도 남아있던 별개의 구멍이었다). 고친 방법: `doMove`를 부르기 전에 `board.legalMoves()`(chesslib의 실제 합법수 생성기, 4절 이동 가능 칸 표시에도 쓰는 바로 그 API)에서 해당 from/to 쌍이 존재하는지부터 확인 — 없으면 곧장 `IllegalMove`로 반환하고 보드는 아예 건드리지 않는다. `core`의 `PuzzleEngineTest`에 이 정확한 시나리오(c6→c5)를 재현하는 회귀 테스트를 추가함.
- 정답(중간 수): 방금 둔 출발/도착 두 칸에 `lastMoveOutline` 강조(칸 안쪽 accent 테두리) → 상대 응수 자동 재생(600ms 애니메이션) → 상단 텍스트가 다음 차례로 갱신.
- 정답(퍼즐 완료): 상단 텍스트 "Correct"(success 색) 0.8초 표시 → 하단 레이팅 칩이 갱신된 값 + `+14` 같은 변화량으로 즉시 갱신 → 자동으로 다음 퍼즐 로딩 상태로 전환(사용자 추가 조작 없이, "화면을 옮기지 않고 끝낼 수 있는 작업은 옮기지 않는다" 원칙).
- 네트워크 오류(블루투스 프록시 끊김 등): 상단 텍스트 `error` 색으로 "Connection Lost"(이제 텍스트 자체는 탭 불가능한 상태 표시일 뿐) + 화면 정중앙에 실제 "Retry" 버튼(Wear Compose Material3 `Button`, 로그인 화면과 동일한 컴포넌트). 하단 레이팅 칩은 다른 상태와 동일하게 마지막으로 알고 있던 값을 계속 표시(연결이 끊겼다고 레이팅 정보까지 가릴 필요는 없음).
  > **구현 중 수정(사용자 요청)**: 원래는 "텍스트 자체가 탭 가능한 재시도 버튼을 겸한다(별도 Retry 요소를 추가하지 않는다)"는 방향이었는데, 화면 맨 위의 작은 텍스트는 재시도 지점으로 눈에 잘 안 띈다는 피드백에 정중앙에 실제 버튼을 추가하는 쪽으로 뒤집었다 — 로딩 중 `CircularProgressIndicator`가 같은 자리(화면 중앙)에 뜨는 것과 같은 배치 규칙이라 새 레이아웃 규칙을 만들 필요는 없었다.
  > **구현 중 수정(로그인 버튼과 동일한 양식으로 — 사용자 요청)**: 두 버튼 모두 같은 `Button` 컴포저블 + 같은 테마(`primary = accent`)를 그대로 쓰고 있어 이미 사실상 같은 모양이었지만, 어느 쪽도 4절에 적어둔 버튼 규격(52dp 높이, 안전영역 70% 폭, 모서리 반경 26dp)을 실제로는 구현하지 않고 기본값에만 기대고 있었다. 이번에 두 버튼 모두에 그 값을 명시적으로 적용해(`Dimens.ButtonHeight`/`ButtonCornerRadius`, `LoginScreen.kt`/`PuzzleScreen.kt`), "우연히 같아 보임"이 아니라 "코드로 보장된 동일함"으로 만들었다 — 로그인 화면의 `Column`도 이제 `fillMaxWidth()`를 둬야 그 안의 버튼이 화면 폭 기준 70%를 정확히 계산할 수 있다(전에는 `Column`이 내용물 크기로만 좁게 잡혀 있었음).
  > **구현 중 수정 2(텍스트 중앙 정렬 + Retry는 텍스트 크기로 — 사용자 요청)**: 두 버튼의 `Text`에 `textAlign = Center` + `Modifier.fillMaxWidth()`를 추가해 버튼 폭 안에서 확실히 중앙 정렬되도록 했다. 다만 **Retry 버튼은 로그인 버튼과 달리 폭을 안전영역의 70%로 고정하지 않고**(그 규칙은 모서리 반경에만 남겨둠) 폭 지정 없이 `"Retry"` 텍스트 크기에 맞춰 저절로 줄어들게 뒀다 — 로그인 버튼의 문구("Sign in with Lichess")보다 훨씬 짧은 단어라 굳이 화면의 70%를 채울 필요가 없다는 사용자 판단. 뒤이어 세로 방향도 마저 줄여달라는 요청에, 로그인 버튼과 맞추려 넣었던 `Dimens.ButtonHeight`(52dp) 고정값도 Retry 버튼에서는 제거했다.
  > **구현 중 수정 3(높이가 여전히 안 줄어듦 — 컴포넌트 자체를 교체)**: 높이 고정값을 지웠는데도 "더 줄여달라"는 요청이 다시 왔다 — 표준 `Button`은 명시적 `Modifier.height()`가 없어도 내부적으로 자기만의 최소 높이를 강제하고 있어서였다. Wear Compose Material3에는 이런 경우를 위한 **`CompactButton`이라는 전용 컴포저블이 따로 있다**(자체적으로 더 작은 기본 높이·패딩을 가짐) — `Button`을 억지로 줄이는 대신 `CompactButton`으로 통째로 교체해 해결했다.
- **좌표 입력**: 우측 탭(⌨) 탭 → `MoveInputLauncher`가 워치 시스템 키보드를 전체화면으로 실행(RESEARCH.md 10-A절) → 입력 결과를 보드 하단 위에 작은 모노스페이스 텍스트 상자로 에코(예: `Nc3`, 커서 깜빡임) → 형식이 올바르면 일반 탭-이동과 동일하게 처리, 형식이 잘못됐거나 비합법수면 오답과 동일한 피드백. **입력 표기법은 UCI(`e2e4`)가 아니라 표준 대수 기보법(SAN, 예: `Nc3`, `exd5`, `O-O`)** — chesslib의 SAN 파싱/생성 기능을 그대로 사용(RESEARCH.md 8절). 이 상태에서도 하단 레이팅 칩은 계속 표시한다(연결 끊김 상태와 동일하게, 입력 중이라고 다른 정보를 가릴 필요는 없음).
- **힌트**(사용자 요청, 4절 참고): 좌측 탭(💡) 첫 탭 → 정답 수의 출발 칸에 테두리 표시. 같은 탭 재탭 → 그 기물을 실제로 이동(정답 처리와 동일 경로, 상대 응수·퍼즐 완료까지 이어짐). 보드를 직접 탭하거나 SAN을 입력하면 힌트 테두리는 취소된다.
- **기물 선택 시 이동 가능 칸 표시**(사용자 요청): 기물을 선택하면 그 기물의 합법수 전부를 즉시 보여준다 — 빈 칸은 칸 중앙의 작은 원, 상대 기물을 잡는 칸은 칸에 내접하는 원(둘 다 `legalDot` 색). 선택한 칸 자체는 `selectedSquare`(밝은/어두운 칸의 RGB 평균, 완전 불투명)로 배경이 바뀐다.
- **드래그로 기물 이동**(사용자 요청): 탭-선택-탭-이동 외에, 자기 기물을 누른 채 끌어서 놓아도 이동한다 — 누른 순간 그 기물을 선택(탭 1회와 동일)한 뒤, 원래 칸 자리엔 기물을 그리지 않고 손가락을 따라다니는 기물 이미지를 그 위에 겹쳐 그리다가, 손을 뗀 칸에 대해 탭-이동과 동일한 처리를 한다. 자기 기물이 아닌 칸에서 시작한 드래그는 아무 동작도 하지 않는다.
- **롱프레스로 확대 후 정밀 탭**(사용자 요청): 화면을 길게 누르고 있으면(약 0.5초) 누른 지점을 중심으로 보드 전체가 2.2배 확대된다. 손을 뗄 때까지 손가락을 움직이면 확대된 화면이 따라 이동한다 — 실제 돋보기처럼. 손을 떼면 그 순간 화면 중앙(확대 변환이 항상 재중심시키는 지점)에 있던 칸에 대해 일반 탭과 동일하게 처리(선택/이동/해제)한 뒤 확대를 원래대로 되돌린다. 작은 원형 화면에서 칸 하나가 손가락 굵기보다 작다는 문제(6절 리스크 1)를 코드 변경 없이 완화하는 수단.
  > **구현 중 수정(패닝 감도 상향 — 사용자 요청)**: 처음엔 "화면 배율만큼 나눈" 손가락 이동량을 그대로 패닝 거리로 썼다(돋보기 물리 법칙과 똑같이). 실제로 써보니 화면 반대편 칸까지 옮겨가기엔 손가락을 너무 많이 움직여야 해서 둔하게 느껴진다는 피드백에, `ZOOM_PAN_SENSITIVITY = 3`을 추가로 곱해 같은 손가락 이동량 대비 3배 더 멀리 패닝하도록 올렸다 — "물리적으로 정확한" 패닝보다 "빨리 원하는 칸에 닿는" 쪽을 택함.
  > **구현 중 수정 2(감도 하향 조정 — 사용자 요청)**: 3배는 반대로 과했다는 피드백에 `ZOOM_PAN_SENSITIVITY`를 `2`로 낮췄다 — 여전히 "물리적으로 정확한" 1배보다는 빠르게 이동하지만, 3배만큼 홱홱 튀지는 않도록 절충.
  > **구현 중 수정**: 처음엔 정확히 어느 칸을 겨냥하고 있는지 보여주려고 화면 중앙에 사각 테두리(reticle)를 함께 그렸는데, 사용자가 이 테두리를 안 보이게 해달라고 요청해 제거했다 — 확대/패닝/정밀 탭 동작 자체는 그대로이고, 시각적 표시만 없앴다.
  > **구현 중 참고**: 탭·드래그·롱프레스 세 제스처가 서로 다른 판정 기준(움직임 없음/즉시 뗌 = 탭, 롱프레스 타임아웃 전에 터치 슬롭을 넘겨 움직임 = 드래그, 아무 것도 안 하고 타임아웃까지 버팀 = 확대)으로 갈리기 때문에, 칸마다 있던 개별 `clickable`을 걷어내고 보드 전체를 감싸는 **하나의** 저수준 제스처 처리기(`awaitEachGesture`, `PuzzleScreen.kt`의 `Board()`)로 합쳤다 — 자식 칸의 `clickable`과 부모의 커스텀 드래그 감지기를 동시에 두면 자식이 먼저 이벤트를 소비해버려 부모가 못 받는 문제가 있다. adb `input swipe`로 테스트하다가 "합성 스와이프가 이동과 뗌을 같은 이벤트에 한번에 담아 보낼 수 있다"는 걸 실제로 겪었다 — 처음엔 "뗐는지"를 "움직였는지"보다 먼저 검사해서 빠른 드래그가 탭으로 오판됐고, 검사 순서를 뒤집어(이동 먼저) 고쳤다.

## 6. 접근성

- 모든 텍스트/배경 조합은 WCAG AA 이상 대비(위 표에 대비비 명시). 기물은 칸 색과 무관하게 항상 반대색 1.5dp 외곽선을 둘러 가독성 확보.
- 버튼 최소 터치 타깃 52dp(48dp 기준 초과 충족).
- **알려진 리스크 1 — 보드 칸 터치 타깃**: 8×8 보드를 원형 480px 화면에 내접시키면(4절, 보드를 최대한 키운 뒤에도) 칸 하나당 변 길이가 Wear 접근성 권장 최소 터치 타깃(48dp)에 못 미칠 가능성이 높음(체스가 8칸 고정이라 대안이 없음). **완화책**: 시각적 칸 경계보다 히트박스를 사방 각 3~4dp 더 넓게 잡아 인접 칸 오탭 확률을 낮추고, 탭 시 해당 칸 전체를 `selectedSquare` 색으로 크게 하이라이트해 사용자가 인식/정정하기 쉽게 한다. 드래그 대신 탭-선택 방식을 쓴 이유이기도 하다(RESEARCH.md 9절).
- **알려진 리스크 2 — 좌표 입력 탭 크기**: 우측 좌표 입력 탭은 시각적 폭이 48dp에 크게 못 미친다(보드 옆 여백에 맞춘 좁은 탭). **완화책**: 시각적 크기와 무관하게 Wear Compose의 `Modifier.minimumInteractiveComponentSize()`로 실제 터치 영역을 48dp까지 확장(다른 요소와 겹치지 않는 범위에서) — 시각적으로는 얇게, 터치 판정은 넓게 유지한다.

## 7. 디자인 기술 스택

- 디자인 도구: 별도 Figma 파일 없이 **Claude가 직접 값 설계 + HTML 목업으로 시각 확인**(2화면짜리 개인용 앱에 Figma 왕복은 비용 대비 이득이 낮다고 판단 — 대안으로 기록).
- 서체: 시스템 기본(Wear OS 기본 Roboto Flex 계열), 무료, 별도 라이선스 확인 불필요.
- 아이콘(기물): 사용자가 직접 제공한 PNG 세트(`LS/`, `DS/`, 2절 참고)에서 기물 12장을 알파 마스크로 잘라낸 비트맵(`app/src/main/res/drawable-nodpi/piece_*.png`) — 라이선스는 제공자 본인이 사용을 허가한 개인 프로젝트 에셋. 추가 외부 라이선스 확인 불필요.
- 차트: 해당 없음(범위 밖).
- 토큰 전달 경로(단일 소스): 색상·타이포·간격 값은 이 문서 → `ui/theme/Color.kt`, `Type.kt`, `Dimens.kt` (Kotlin object 상수)로 직접 옮겨 적음. 기물 이미지는 최종 확정 후 원본(`LS/`·`DS/`)과 추출 스크립트를 저장소에서 정리했다(2절 "구현 중 수정 8") — **`res/drawable-nodpi/piece_*.png` 12장 자체가 지금의 단일 소스**다. 외부 동기화 도구 불필요.
- 구현 비용이 큰 요소와 대안: 커스텀 폰트는 채택하지 않고 시스템 폰트로 대체(위 서체 항목에 근거 기록).
- 좌표 입력: 별도 UI 프레임워크 없이 `androidx.wear:wear-input`의 `RemoteInputIntentHelper`로 워치 시스템 입력 선택기(키보드/딕테이션/손글씨)를 그대로 사용 — 커스텀 키패드를 직접 그리지 않음(RESEARCH.md 10-A절, PLAN.md 6절 트레이드오프 참고).

## 8. 산출물

아래 HTML 목업(로그인/퍼즐 화면 + 로딩/오답/정답/오류 상태)을 Artifact로 게시함: https://claude.ai/code/artifact/bd94fc50-0e8f-4b04-82c4-21da4f42cc62
